/*
	Level

	- A primitive static container that holds a list of level entities
*/

#pragma once
#ifndef LEVEL
#define LEVEL
#ifdef	DT_CORE_EXPORT
#define DELTA_CORE_API __declspec(dllexport)
#else
#define	DELTA_CORE_API __declspec(dllimport)
#endif

namespace LEVEL {

}

#endif // LEVEL